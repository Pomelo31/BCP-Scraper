# Dependencias para el Bot de Web Scraping del Banco Central del Paraguay

# Librería principal para hacer requests HTTP
requests>=2.31.0

# Parser HTML para web scraping
beautifulsoup4>=4.12.0

# Parser XML alternativo (más rápido que el parser por defecto)
lxml>=4.9.0

# Para manejo de archivos Excel (opcional, para validación)
openpyxl>=3.1.0

# Para manejo de fechas y timestamps
python-dateutil>=2.8.0

# Para logging mejorado (opcional)
colorlog>=6.7.0

# Para manejo de configuración (opcional)
configparser>=5.3.0

# Para validación de URLs
validators>=0.20.0

# Para manejo de JavaScript y navegador real
requests-html>=0.10.0
cloudscraper>=1.2.71

# Para simulación de navegador más avanzada
selenium>=4.15.0
